# Images

Upload photos here (hardware teardown, UART TX/RX wiring, the `Aes PWd:` boot log,
the Wi‑Fi module, the AC unit, Home Assistant screenshots, etc.).

They can be referenced from the docs, e.g.:

```markdown
![UART wiring](images/uart-wiring.jpg)
```
